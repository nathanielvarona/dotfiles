# ============================================================
# Global ignores (all platforms)
# ============================================================

.editorconfig
.tmuxp.yaml
install.ps1
install.sh
Justfile
LICENSE
README.md

.github
.gitmodules
pkgs
docs

**/lazy-lock.json

# ============================================================
# Platform-specific ignores
# ============================================================

# ============================================================
# macOS and Linux ignores
# ============================================================

{{- if or (eq .chezmoi.os "darwin") (eq .chezmoi.os "linux") }}

# Ignore Windows-only configs
AppData
Documents

{{- end }}

# ============================================================
# macOS-specific ignores
# ============================================================

{{- if eq .chezmoi.os "darwin" }}

{{- end }}

# ============================================================
# Linux-specific ignores
# ============================================================

{{- if eq .chezmoi.os "linux" }}

# macOS-specific leftovers
.MacOSX
.scripts

{{- end }}

# ============================================================
# Windows-specific ignores
# ============================================================

{{- if eq .chezmoi.os "windows" }}

# Unix-only configs
.MacOSX
.zshenv

.config/{aerospace,alacritty,asdf,atuin,fzf,gh-dash,ghostty,kitty,luarocks,opencode,powerlevel10k,gnupg,scooter,tmux,ueberzugpp,vim,wezterm,yazi,zed,zellij,zsh}

.scripts

{{- end }}
